setup purpose-personal
implement opts.purpose so that switching between purposes requires just changing the imported module, and nothing else
idea is: no need to uncomment tons of modules when changing purpose (for testing or whatever).
setup syncthing and have it work better than last time
clean code up
all the already existing todos
